## What changed

## Why

## Screenshots / clips (UI changes)

## Checklist

- [ ] `git status` shows no secrets (`.env`, `dsk/cookies.json`, `data/*.json`)
- [ ] Tested locally (`python main.py` and/or Electron)
- [ ] Docs updated (README / CHANGELOG) if behavior changed
